1. place all files in the same directory
2. run the main.py file. There are lines that are commented out because they create the database/data files.
    If you would like to, feel free to uncomment but that will affect the data in unwanted ways
3. to run tests/coverage, run "coverage run -m pytest" in command line. all 8 test cases will be passed.